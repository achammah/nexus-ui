# nexus-ui — component catalog

GENERATED by `node scripts/gen-docs.mjs` (vendor set of 2026-07-19) from the live tree — edit the WHEN dictionary in the script, never this file. Machine-readable twin: `docs/catalog.json`. Deep docs: `docs/record-core.md` (the config-driven record system) · `docs/viewer3d.md` (the 3D object / floor-plan surface) · `AGENTS.md` (how an agent works in this repo).

126 entries: 48 vendored shadcn items + wrappers + record-core + tokens.

| Component | Kind | Import | When to use |
|---|---|---|---|
| accordion | vendored | `src/components/ui/accordion.tsx` | collapsible Q&A / grouped detail sections; one or many open |
| alert | vendored | `src/components/ui/alert.tsx` | inline callout (info/destructive) inside a page — not a toast, not a dialog |
| alert-dialog | vendored | `src/components/ui/alert-dialog.tsx` | REVIEW SURFACE before an irreversible action (delete/send) — names the target, confirm/cancel; required before any bulk mutation |
| aspect-ratio | vendored | `src/components/ui/aspect-ratio.tsx` | media/embed boxes that must keep a ratio while resizing |
| avatar | vendored | `src/components/ui/avatar.tsx` | person/org identity dot with image + initials fallback |
| badge | vendored | `src/components/ui/badge.tsx` | status chip; prefer the primitives Badge wrapper for Nexus tones (ok/warn/danger/accent) |
| breadcrumb | vendored | `src/components/ui/breadcrumb.tsx` | hierarchical location trail (Settings / Team / Member) |
| button | vendored | `src/components/ui/button.tsx` | every action; prefer the primitives Button wrapper (variant/size/busy/icon API) |
| calendar | vendored | `src/components/ui/calendar.tsx` | date picking (react-day-picker); pairs with popover for a date-picker field |
| card | vendored | `src/components/ui/card.tsx` | raised content container with header/footer slots; .nxCard is the lighter token twin |
| carousel | vendored | `src/components/ui/carousel.tsx` | horizontally swiped media/item strip (embla) |
| chart | vendored | `src/components/ui/chart.tsx` | recharts wrapper bound to the token palette (--chart-1..5); dashboards + trend blocks |
| checkbox | vendored | `src/components/ui/checkbox.tsx` | boolean field / row selection; wrapper Checkbox gives the simple checked/onChange API |
| collapsible | vendored | `src/components/ui/collapsible.tsx` | show-more region without accordion semantics |
| command | vendored | `src/components/ui/command.tsx` | typeahead lists + the ⌘K palette (cmdk); the starter wires CommandPalette on it |
| context-menu | vendored | `src/components/ui/context-menu.tsx` | right-click menus on rows/cards |
| dialog | vendored | `src/components/ui/dialog.tsx` | modal for a focused task (create/edit form); wrapper Dialog gives title/footer API |
| drawer | vendored | `src/components/ui/drawer.tsx` | bottom sheet on mobile-ish flows (vaul) |
| dropdown-menu | vendored | `src/components/ui/dropdown-menu.tsx` | kebab/row action menus; wrapper Menu gives trigger+items API |
| field | vendored | `src/components/ui/field.tsx` | labeled field scaffolding (Field/FieldGroup/FieldLabel…) used by newer blocks; form.tsx remains the react-hook-form wiring |
| form | vendored | `src/components/ui/form.tsx` | react-hook-form + zod field wiring (FormField/FormItem/FormMessage) for validated forms |
| hover-card | vendored | `src/components/ui/hover-card.tsx` | rich preview on hover (person card over a mention) |
| input | vendored | `src/components/ui/input.tsx` | single-line text entry; wrapper Input adds invalid→aria-invalid |
| input-otp | vendored | `src/components/ui/input-otp.tsx` | segmented one-time-code entry |
| label | vendored | `src/components/ui/label.tsx` | accessible field labels tied to inputs |
| menubar | vendored | `src/components/ui/menubar.tsx` | app-level horizontal menu (File/Edit style) |
| navigation-menu | vendored | `src/components/ui/navigation-menu.tsx` | marketing/site-style top navigation with panels |
| pagination | vendored | `src/components/ui/pagination.tsx` | page-by-page navigation for long lists (record-core tables usually filter instead) |
| popover | vendored | `src/components/ui/popover.tsx` | small anchored panel (filter builder, emoji picker); not a menu, not a dialog |
| progress | vendored | `src/components/ui/progress.tsx` | determinate progress bar; pair with async-run surfaces |
| radio-group | vendored | `src/components/ui/radio-group.tsx` | single choice among few visible options |
| resizable | vendored | `src/components/ui/resizable.tsx` | split panes with draggable handles (react-resizable-panels) |
| scroll-area | vendored | `src/components/ui/scroll-area.tsx` | styled scroll container for bounded internal scroll |
| select | vendored | `src/components/ui/select.tsx` | single choice among many options (composed listbox); raw <select class=nxInput> stays fine for tiny enum cells |
| separator | vendored | `src/components/ui/separator.tsx` | hairline divider |
| sheet | vendored | `src/components/ui/sheet.tsx` | side panel over content (record peek, filters) — the ZOOM step without leaving the list |
| sidebar | vendored | `src/components/ui/sidebar.tsx` | full app-shell sidebar system (collapsible groups, mobile behavior); the starter shell is a lighter hand-rolled twin — adopt this when an app outgrows it |
| skeleton | vendored | `src/components/ui/skeleton.tsx` | loading placeholders shaped like the content |
| slider | vendored | `src/components/ui/slider.tsx` | numeric range input |
| sonner | vendored | `src/components/ui/sonner.tsx` | toast system (Toaster + toast()); the starter ships its own minimal toast — pick ONE per app |
| switch | vendored | `src/components/ui/switch.tsx` | on/off setting with immediate effect |
| table | vendored | `src/components/ui/table.tsx` | styled table primitives; record-core DataTable is the batteries-included grid on top |
| tabs | vendored | `src/components/ui/tabs.tsx` | in-page view switching; wrapper Tabs gives the {tabs,value,onValueChange} API |
| textarea | vendored | `src/components/ui/textarea.tsx` | multi-line text entry |
| toggle | vendored | `src/components/ui/toggle.tsx` | pressed/unpressed tool button |
| toggle-group | vendored | `src/components/ui/toggle-group.tsx` | exclusive or multi toggle row (view switches) |
| tooltip | vendored | `src/components/ui/tooltip.tsx` | hover hint; wrapper Tip gives label+child API |
| use-mobile | vendored-hook | `src/hooks/use-mobile.ts` | hook: viewport-below-768 boolean (sidebar dependency) |
| login-03 | block | `src/blocks/login-03/page.tsx · src/blocks/login-03/login-form.tsx` | copy-out starting screen: card login with provider slots — richer than the starter's built-in gate |
| sidebar-07 | block | `src/blocks/sidebar-07/page.tsx · src/blocks/sidebar-07/app-sidebar.tsx · src/blocks/sidebar-07/nav-main.tsx · src/blocks/sidebar-07/nav-projects.tsx · src/blocks/sidebar-07/nav-user.tsx · src/blocks/sidebar-07/team-switcher.tsx` | copy-out starting shell: collapsible sidebar app frame (breadcrumb header, icon rail) — adopt when an app outgrows the starter's hand-rolled shell |
| Button | wrapper | `src/primitives/Button.tsx` | the house button API (variant primary/secondary/ghost/danger · size sm/md · busy · icon) over vendored button |
| Input · Badge · Micro · Tabs/TabPanel · Checkbox · Tip | wrapper | `src/primitives/fields.tsx` | house APIs over vendored input/badge/tabs/checkbox/tooltip + the Micro eyebrow |
| Dialog · Menu | wrapper | `src/primitives/overlays.tsx` | house APIs over vendored dialog/dropdown-menu (title+footer / trigger+items) |
| SettingsTabs | wrapper | `src/primitives/SettingsTabs.tsx` | generic settings shell: sticky tab bar (tabs-as-config: key/label/icon/render) + body, own active-tab state — page head stays app content |
| EditableRuleList | wrapper | `src/primitives/EditableRuleList.tsx` | config-driven editable rule/policy list: inline add/edit card, optional severity chip (enum+colors as props) and active toggle, over any object — no fetch inside, onCreate/onPatch/onRemove callback contract like DataTable |
| ThinkingDots | wrapper | `src/primitives/ThinkingDots.tsx` | the 'AI is working' indicator (three bouncing accent dots); tokenized + reduced-motion aware — drop in beside any agent/task-run affordance |
| Markdown · renderMarkdown | wrapper | `src/primitives/Markdown.tsx` | dependency-free Markdown → React renderer (inline bold/italic/code + block headings/lists/quotes/fenced code/tables/rules); tokenized in markdown.css — for agent replies and short rich text |
| DataTable | record-core | `src/record-core/DataTable.tsx` | config-driven grid: sort, selection, inline edit, relation link cells, numeric alignment — see docs/record-core.md |
| KanbanBoard | record-core | `src/record-core/KanbanBoard.tsx` | config-driven board over the object's stageField; drag or stage-select moves cards |
| RecordPage | record-core | `src/record-core/RecordPage.tsx` | record anatomy: header + fields panel (inline edit, enrich affordance) + Timeline (activity composer, per-kind icons) / Notes / Files tabs |
| NotionEditor | record-core | `src/record-core/NotionEditor.tsx` | Notion-grade block editor for the richText field type: hand-rolled contenteditable blocks (p/h/quote/list/divider/image/table), slash menu, drag-reorder, inline markdown, image-drop; controlled (blocks/onChange) + inline-change props for a suggestions layer |
| useSuggestions · Suggestion | record-core | `src/record-core/useSuggestions.ts` | the accept/reject/undo engine for inline tracked changes over a Block[] document: folds an accepted change's original into replacement in the doc, tracks each change's status; entity-agnostic (no object/api coupling), pairs with SuggestionPanel + NotionEditor's inline-change props |
| SuggestionPanel | record-core | `src/record-core/SuggestionPanel.tsx` | the review rail for inline tracked changes: one card per change (del to ins diff + reason), accept/reject on pending + undo on resolved, bulk accept-all/reject-all, progress; pure presentational, wired to the useSuggestions callbacks |
| Pipeline · Chip | record-core | `src/record-core/Pipeline.tsx` | generic horizontal state indicator over a config-declared set of states (done/current/in-progress steps) plus the Chip status pill it is built from; states are plain strings the caller supplies |
| FilterBar · FilterChips · matchFilters · opsFor · filterableFields | record-core | `src/record-core/Filters.tsx` | advanced filtering: command-style filter builder (any column, type-aware operators) + removable active-filter chips + a pure matchFilters(row,conds) predicate; filterableFields(fields) maps an object's fields to the filterable set |
| ChartView | record-core | `src/record-core/ChartView.tsx` | config-driven chart: one bar per group option, Count or Σ numeric measure — the third view family beside table/board |
| ViewDefinition · ViewProps · ViewInstanceConfig | record-core | `src/record-core/views/types.ts` | the view-type contract: what a views/<type>/definition.tsx default-exports (label/icon/component/toolbar/config schema) and the props every view receives from its host |
| viewDefinitions · getViewDefinition | record-core | `src/record-core/views/registry.ts` | build-time self-registering view registry (import.meta.glob over views/*/definition) — drop a folder = a new view type in every consumer's switcher, zero switcher edits |
| buildRegistry · configuredViewsFor | record-core | `src/record-core/views/resolve.ts` | the registry's pure core (no browser, no vite — node-testable): fold glob modules into the type map; derive an object's view tabs (declared `views` or the legacy table/board/chart set) |
| groupableFields · measurableFields · resolveGroupBy | record-core | `src/record-core/views/group.ts` | shared grouping derivations for board/chart views and their host: which fields can group, which can measure, and the groupBy fallback chain (runtime pick → instance config → stageField → first groupable) |
| GroupByMenu | record-core | `src/record-core/views/controls.tsx` | the shared group-by picker rendered by both board and chart toolbars (same viewState.groupBy — regroup one, the other follows) |
| table view definition | record-core | `src/record-core/views/table/definition.tsx` | the built-in table view: DataTable behind the ViewProps contract + the Columns visibility menu as its toolbar |
| gallery view definition | record-core | `src/record-core/views/gallery/definition.tsx` | the built-in gallery view: cover-card masonry (coverField/titleField/metaFields/cardSize config) — cards open the peek; lazy component |
| packColumns · visibleIndices · cardHeight · columnCountForWidth | record-core | `src/record-core/views/gallery/pack.ts` | the gallery's pure masonry math (node-testable): deterministic shortest-column packing over exact card heights + viewport windowing |
| form view definition | record-core | `src/record-core/views/form/definition.tsx` | the built-in form view: config-driven fill-one-record intake (fields/requiredOverrides/submitLabel/successMode) submitting through ViewProps.onCreate; lazy component |
| coerceDraft · validateDraft · withStageDefault · requiredKeys · coerceScalar · listValidators · formSupported | record-core | `src/record-core/fields/draft.ts` | the draft pure core every create surface shares (dialog, wizard, form): typed-string coercion, config-implied validation mirroring the server, stage defaulting; registered field types plug their coerce/validate slots into the same pipeline |
| Draft* editors · MoneyField · ListField · AddressField · FullNameField · MultiSelectField · ArrayField · DateField | record-core | `src/record-core/fields/editors.tsx` | the per-type field editors: draft-mode controlled editors for every built-in type plus the shaped record-page editors (moved here from RecordPage, which re-exports them) |
| fieldDraftEditor | record-core | `src/record-core/fields/draft-resolve.ts` | host-side draft-editor lookup over the field-type registry, text-editor fallback for unregistered types |
| built-in field-type definitions (22 folders) | record-core | `src/record-core/fields/text/definition.ts` | one fields/<type>/definition.ts per built-in type registering its Draft editor on the field-type registry — the pattern a NEW field type copies |
| kanban view definition | record-core | `src/record-core/views/kanban/definition.tsx` | the built-in board view: KanbanBoard behind the ViewProps contract + group-by and per-column rollup pickers as its toolbar |
| chart view definition | record-core | `src/record-core/views/chart/definition.tsx` | the built-in chart view: ChartView behind the ViewProps contract + measure and group-by pickers as its toolbar |
| grid view definition | record-core | `src/record-core/views/grid/definition.tsx` | the Sheet view: Excel-grade bulk editing over an object's records (glide-data-grid) — fill-handle, range select, TSV copy/paste, keyboard cell-nav, frozen primary column; the component ships as a lazy chunk |
| SpreadsheetView | record-core | `src/record-core/views/grid/SpreadsheetView.tsx` | the grid view surface: columns from the object's fields, native + custom (select/multiselect/user chip) cells, every commit one merged onPatch per touched row, paste/fill coerced per target field, row markers synced to the host bulk bar |
| cellDescFor · isGridEditable · coerceFromText · textForCopy | record-core | `src/record-core/views/grid/cells.ts` | the grid's pure cell-mapping core (no glide, no browser — node-testable): field type → cell kind, the editable set, paste/fill text coercion that rejects impossible values |
| deriveSelectionText · relLuminance · contrastRatio | record-core | `src/record-core/views/grid/contrast.ts` | the grid's pure selection-contrast core (no glide, no browser — node-testable): when a skin or theme leaves the selection background too close to the base ink, derive the flipped text ramp (WCAG luminance, white/black pole) that SpreadsheetView applies as a per-cell themeOverride on selected cells |
| useGridTheme · deriveGridTheme · deriveSelectionText · chipColorLiterals · resolveCssColor | record-core | `src/record-core/views/grid/theme.ts` | token → glide Theme derivation for canvas paint: probe-resolved --nx-* literals, chip colors from the exact chipStyle color-mix formula, and the selected-cell contrast text ramp — all re-derived on dark-mode flip and skin arrival |
| selectRenderer · multiselectRenderer · userRenderer | record-core | `src/record-core/views/grid/renderers.tsx` | custom canvas cells for select/multiselect/user: chip and avatar drawing from pre-resolved literals + DOM overlay editors sharing the DOM chip styling |
| provideGridEditor | record-core | `src/record-core/views/grid/editors.tsx` | replacement overlay editors for glide's built-in text/uri/number cells: commit from local state via onFinishedEditing (the library's own commit chain drops values under React 18 StrictMode), guarded blur-commit for click-away |
| flow view definition | record-core | `src/record-core/views/flow/definition.tsx` | the flow (node-graph) view registry entry: records-as-graph config (relationField/labelField), plain-language validation, and the relation-picker toolbar; the canvas loads as a lazy chunk |
| FlowView | record-core | `src/record-core/views/flow/FlowView.tsx` | the records-as-graph canvas (lazy): record cards + cross-object hub chips as xyflow nodes, one configured relation as edges, pan/zoom/minimap/drag-arrange with per-relation drag positions persisted in the view-state bag |
| buildGraph · resolveRelation · resolveLabelField · positionsFor · positionsPatch | record-core | `src/record-core/views/flow/graph.ts` | the flow view's pure core (node-testable, JSX-free): relation resolution, node/edge derivation from the _refs id decoration (self-relation record edges, cross-object/polymorphic hubs), drag-position persistence shapes |
| layoutGraph · bfsGrid | record-core | `src/record-core/views/flow/layout.ts` | flow auto-layout: dagre TB up to 2,000 nodes, an O(V+E) BFS-rank grid above it — imported only by the lazy canvas chunk so dagre stays out of the eager bundle |
| calendar view definition | record-core | `src/record-core/views/calendar/definition.tsx` | the calendar view (FullCalendar month/week, lazy chunk): drag-to-reschedule PATCHes the record's date field, empty-day click seeds a prefilled create, event colors come from the colorField's own select-option palette; Month/Week toggle as its toolbar; mobile swaps the grid for a virtualized agenda list |
| rowsToEvents · patchForDrop · patchForResize · firstDateField | record-core | `src/record-core/views/calendar/events.ts` | pure record→event mapping for the calendar view (node-testable): date vs dateTime semantics, inclusive⇄exclusive span-end conversion, malformed-data normalization, agenda day grouping |
| map view definition | record-core | `src/record-core/views/map/definition.tsx` | the map view: records with lat/lng as token pins (≤25) or GL clusters, record-card popups into the peek, a without-location count chip; config latField/lngField/titleField/colorField — the heavy component is a lazy chunk, maplibre loads only when a map renders |
| CLUSTER_THRESHOLD · inferCoordFields · splitRows · toFeatureCollection · boundsOf · mapDefaultConfig · mapValidateConfig | record-core | `src/record-core/views/map/geo.ts` | the map view's pure geo core (no browser — node-testable): coordinate inference + validity (0 is a valid coordinate), the located/without split, GeoJSON + bounds math, config default/validate |
| RecordCard | record-core | `src/record-core/RecordCard.tsx` | shared compact record card (title + meta values over formatCell/OptionChip) for record-as-card surfaces — map popups, gallery tiles; chrome-less, the host provides the card shell |
| ObjectConfig · FieldDef · RecordRow · ViewDef · TimelineEvent · FileMeta | record-core | `src/record-core/types.ts` | the config-driven object model — the schema every record surface renders from |
| FieldTypeDefinition · FieldRenderProps · FieldCellProps · FieldDraftProps | record-core | `src/record-core/fields/types.ts` | the field-type contract: what a fields/<type>/definition.tsx default-exports — record-page render, list cell, previewText, layout/filterable/keyboard capabilities, plus the Draft/coerce/validate editor slots |
| fieldTypeDefinitions · getFieldTypeDefinition · fieldPreviewText · fieldIsBlock | record-core | `src/record-core/fields/registry.ts` | build-time self-registering field-type registry (import.meta.glob over fields/*/definition) — hosts consult it BEFORE their built-in switches, so a new field type is a dropped folder, zero switch edits |
| buildFieldRegistry · registry capability helpers | record-core | `src/record-core/fields/resolve.ts` | the field registry's pure core (no browser, no vite — node-testable): fold glob modules into the type map + one-line capability reads that tolerate unregistered types |
| whiteboard field type | record-core | `src/record-core/fields/whiteboard/definition.tsx` | a per-record excalidraw canvas as a FIELD: lazy full editor on the record page (debounced one-patch saves, theme-follow, mobile tap-to-edit overlay), memoized theme-aware SVG thumbnails in cells, scene JSON through the normal store path |
| Wizard | wrapper | `src/blocks/wizard/Wizard.tsx` | config-driven guided flow: question set (kinds select/text/long/list/sources) → step engine with progress + slide anim → onComplete; a guided-vs-blank landing; host supplies questions + callbacks |
| ModalOverlay | wrapper | `src/blocks/wizard/ModalOverlay.tsx` | lightweight centered overlay shell (backdrop + escape/close) for a full-page-or-popup wizard |
| ChipListInput | wrapper | `src/blocks/wizard/ChipListInput.tsx` | chip composer: type + enter to add removable tag chips — the list-kind wizard input, reusable standalone |
| SourcesInput | wrapper | `src/blocks/wizard/SourcesInput.tsx` | URL list + file-pick source composer (readFile overridable) — the sources-kind wizard input |
| CopilotPanel · CopilotToggle | wrapper | `src/blocks/copilot/CopilotPanel.tsx` | docked AI copilot side-panel: conversation state + Markdown replies + tool-use chips (host injects the transport + per-turn context), plus the toggle button; config-driven (title/mark/emptyStateCopy/suggestions) |
| ShortcutsOverlay | wrapper | `src/blocks/mobile/ShortcutsOverlay.tsx` | keyboard-shortcuts help modal (host toggles it, typically on `?`): host passes shortcut GROUPS — a core shell group vs an app/config-driven group — the overlay renders the reference and claims Escape/? in capture while open; pairs with the starter's keyboard-nav layer |
| MobileReviewBanner | wrapper | `src/blocks/mobile/MobileReviewBanner.tsx` | generic phone bottom step-through: prev / index-of-total / next over a set of N items plus per-item action buttons (accept/reject/default tones); pure presentation, fixed above a bottom tab bar (--nx-mobilenav-h), hidden on desktop — e.g. stepping a record set or suggestions on mobile |
| WorkbookSurface · LazyWorkbookSurface | wrapper | `src/blocks/workbook/WorkbookSurface.tsx` | full spreadsheet workbook (Univer) rendered as a NATIVE page surface: formula bar + 400+ functions, insert/delete rows & columns, cell formatting, multiple sheets, freeze + merge, in a single-row toolbar (ribbonType simple, overflow-managed) with no card frame; host controls ride the toolbar row via the actions prop. Every piece of vendor chrome (toolbar, formula bar, sheet tabs, menus, portaled popups) resolves --nx-* tokens in light AND dark and re-derives live on theme/skin changes; free-surface (host owns the snapshot via value/onChange). The engine loads ONLY behind LazyWorkbookSurface (a ~1.5MB-gz lazy chunk), so importing the block adds nothing to the eager bundle |
| workbookStoreKey · isWorkbookSnapshot · seedWorkbook · seedLargeWorkbook | wrapper | `src/blocks/workbook/snapshot.ts` | the workbook block's pure core (no @univerjs runtime, node-testable): app_state store-key namespacing, snapshot validation for a stored workbook blob, and the demo + 10k scale seed generators |
| deriveWorkbookTheme · accentScale · neutralScale · withLightTokens · themeSignature · useThemeNonce | wrapper | `src/blocks/workbook/workbook-theme.ts` | token → Univer theme derivation: accent, warm neutral and semantic scales from the live --nx-* tokens, resolved LIGHT-anchored to rgb (withLightTokens forces a light-resolution probe; Univer's canvas derives dark by inverting the light palette) and re-set through Univer's ThemeService on a data-theme / skin flip (themeSignature dedupes observer echoes); canvasGridTheme themes the GRID CANVAS itself (faint gridlines via the renderConfig hook, header paint via setCustomHeader, the freeze divider via gray.300) and re-derives with skins; pairs with the per-mode --univer-* value tables in workbook.css that theme all DOM chrome including portaled menus |
| Viewer3DSurface · LazyViewer3DSurface | wrapper | `src/blocks/viewer3d/Viewer3DSurface.tsx` | 3D viewer as a NATIVE page surface, two modes from one config: OBJECT (glTF/GLB or a procedural preset — orbit/zoom/pan, 360° spin, auto-rotate, front/side/top/iso camera presets with eased flights, wireframe, auto-fit framing, min/max distance + polar clamps so you cannot fly inside or under the model) and FLOORPLAN (walls extruded from room polygons, multi-level switcher that ghosts inactive floors, room labels, top-down ↔ 3D toggle). Hotspots are DATA-DRIVEN (config/records), pinned to world points, occlusion-aware (faded when behind geometry) and open a detail card. Loading / error / retry states; keyboard camera control (arrows orbit, +/- zoom, R reset) and prefers-reduced-motion honored (spin + auto-rotate withdrawn, camera moves snap). Scene colors derive from live --nx-* tokens and re-derive on a theme/skin flip; the canvas is transparent so the stage ground stays pure CSS. Free-surface (host owns the snapshot via value/onChange/reloadNonce). The three.js engine loads ONLY behind LazyViewer3DSurface (a ~174kB-gz lazy chunk), so an app declaring no 3D page pays ~0 eager bytes |
| viewer3dStoreKey · isViewer3dSnapshot · seedScene · Viewer3DSnapshot | wrapper | `src/blocks/viewer3d/scene.ts` | the viewer3d block's pure core (no three.js runtime, node-testable): the whole viewer as ONE config/snapshot blob (mode, model source, floorplan levels+rooms, hotspots, control toggles), app_state store-key namespacing, snapshot validation for a stored blob, and the two seeded demos (a vehicle with damage hotspots + a two-level house plan) that double as journey fixtures |
| LOOK · derivePalette · PRESET_DIRS · fitFor | wrapper | `src/blocks/viewer3d/look.ts` | the viewer's look/feel dial-board: ONE object holding every lighting, exposure, shadow, material-response and camera-framing/easing parameter (the surface and builders carry no magic numbers), plus the token → scene-color palette. Tuning how the viewer LOOKS means editing this file — wall/floor legibility mixes, env intensity per theme, ground-shadow strength, per-mode framing direction and distance, orbit damping and fly/spin durations |
| buildSedan · buildLevel · setLevelGhost | wrapper | `src/blocks/viewer3d/builders.ts` | procedural geometry for the two demo scenes: a sedan generated at runtime from an extruded side silhouette + greenhouse, wheels, mirrors and emissive lamps (zero asset bytes, license-free, CSP-safe), and a floor level extruded from room polygons (floor slabs, wall boxes, crisp edge outlines, room-centroid label anchors) with the ghosting pass for inactive levels |
| Skin · skinToCss() · applySkin() | skins | `src/skins/skin.ts` | brand-as-data: a small JSON in, the full --nx-* set out (accent ramp, chrome, radius, fonts, labels, density, shadows, dark derivation, raw overrides) — see docs/THEMING.md |
| skinPresets (nexus · ember · warm-opt) | skins | `src/skins/presets.ts` | built-in skins: the house identity, a full-range org example (dark chrome, sharp corners, own palette/type), and a warm option-chip palette preset (skin-overridable; default palette unchanged) |
| --nx-* custom properties | tokens | `src/tokens/tokens.css` | the blank canvas: palette/type/geometry/motion + chrome/label tokens; light+dark first-class; skins WRITE this at runtime, an app's design lock edits it statically |
| nx motion utilities | tokens | `src/tokens/motion.css` | riseIn/popIn entrance families + .nx-tap-scale/.nx-hover-lift micro-interactions, tokenized on the ease vars; ONE reduced-motion guard (opt back in with data-motion="always") |
| resolveCssColor · resolveTokenColor · useTokenColors · subscribeTokenColors | tokens | `src/tokens/resolve.ts` | token → rgba-literal resolution for GL/canvas consumers that cannot read CSS custom properties (maplibre paint, canvas grids); resolves any color expression incl. color-mix chains, re-resolves live on data-theme / skin-tag / OS-scheme changes |
| shadcn semantic bridge | tokens | `src/styles/shadcn.css` | maps shadcn variables onto --nx-* + Tailwind v4 @theme; import AFTER tokens.css |
| usePollRev | hook | `src/hooks/usePollRev.ts` | live-sync: poll a revision counter, fire onChange when another writer bumped it (transport-agnostic fetchRev; pauses while the tab is hidden) |
| useAsyncOp · computeAsyncOp | hook | `src/hooks/useAsyncOp.ts` | drive a long off-machine op with a stall guard: poll while in-flight, onSettle once, stalled past a threshold (injectable clock) |
| useDebouncedSave · createDebouncer | hook | `src/hooks/useDebouncedSave.ts` | debounced autosave: coalesce rapid edits into ONE persist + a saveState (idle→saving→saved) |
| cn() | lib | `src/lib/utils.ts` | class merge (clsx + tailwind-merge) — the shadcn contract |

## Rules of the road
- **Never edit `src/components/ui/*`** — vendored verbatim; re-vendoring overwrites. House opinions live in `src/primitives/` wrappers.
- Styling changes go through **tokens** (`src/tokens/tokens.css`) — the shadcn bridge derives from them; a design lock restyles the whole kit by editing tokens only.
- A control missing here is added to THIS library (extend `scripts/vendor-shadcn.mjs` COMPONENTS or write a wrapper), never hand-built in one app.
